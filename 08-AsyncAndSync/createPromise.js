const myPromise = new Promise((resolve, reject) => {

    // Asychronous operation goes here

    // if successful , call resolve operation with the result

    // if failure and there is an error , call reject with the error
     
})